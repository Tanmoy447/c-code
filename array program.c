#include<stdio.h>
int main(){
  int arr[]={1,2,3,4,5};
  int length =size ofarr/size of (arr[0]);
  printf("elements of given array:\n);
  for (int i=0,i<length,i++)
  {
  printf("%d,arr[i]")
  }
return 0;
}
